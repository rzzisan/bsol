# ডোমেইন/সাবডোমেইন — নিরাপত্তা অডিট রিপোর্ট

**তারিখ:** ২০২৬-০৮-১৫
**পরিধি:** per-seller subdomain ফিচার (D1–D5) ও তার সহায়ক অংশ — wildcard DNS/TLS, nginx routing, Next.js proxy, login handoff, ল্যান্ডিং পেজ resolution, reserved subdomain, admin impersonation।
**পদ্ধতি:** সোর্স কোড পাঠ + লাইভ প্রোডাকশন সার্ভারে সক্রিয় probe। প্রতিটি দাবি পরীক্ষা করে যাচাই করা, ডকুমেন্টেশন থেকে অনুমান করা নয়।
**সীমা:** এটি কোনো তৃতীয়-পক্ষ পেনিট্রেশন টেস্ট নয়। শুধু ডোমেইন-সংক্রান্ত surface দেখা হয়েছে — courier/SMS/bKash/Facebook ইন্টিগ্রেশনের নিজস্ব surface এর বাইরে।

---

## সারসংক্ষেপ

| তীব্রতা | সংখ্যা | অবস্থা |
|---|---|---|
| Critical | ০ | — |
| High | ০ | — |
| Medium | ২ | **দুটোই এই অডিটে ঠিক করা হয়েছে** |
| Low | ৪ | ৩টি গ্রহণযোগ্য ঝুঁকি হিসেবে নথিভুক্ত, ১টি সুপারিশ |
| তথ্যমূলক | ৩ | — |

**সামগ্রিক রায়:** ডোমেইন surface-এ কোনো critical বা high ঝুঁকি পাওয়া যায়নি। দুটি medium ত্রুটি পাওয়া গেছে ও একই সেশনে সংশোধন করা হয়েছে। মূল স্থাপত্যগত সিদ্ধান্তগুলো (per-origin token, user-derived URL, tombstone, is_system সুরক্ষা) নিরাপত্তার দিক থেকে সঠিক এবং পরীক্ষায় টিকেছে।

---

## ১. যা ঠিক পাওয়া গেছে (যাচাইকৃত)

### ১.১ ক্রস-সেলার অ্যাকাউন্ট দখল — কাঠামোগতভাবে অসম্ভব
Auth টোকেন `localStorage`-এ, যা **origin-প্রতি সম্পূর্ণ আলাদা**, আর কোনো session কুকি ব্যবহার হয় না (`SESSION_DOMAIN=null`)। ফলে `seller2.zyrotechbd.com`-এর কোনো স্ক্রিপ্ট `seller1`-এর টোকেন পড়তে পারে না — ব্রাউজারের same-origin policy এটা নিশ্চিত করে, আমাদের কোডের সঠিকতার উপর নির্ভর করে না। সেলার নিজের ল্যান্ডিং পেজে যা-ই ইনজেক্ট করুক, তা শুধু নিজের origin-এ চলে।

### ১.২ Impersonation দিয়ে অ্যাকাউন্ট দখল করা যায় না
পাসওয়ার্ড পরিবর্তনে `current_password` বাধ্যতামূলক ([AuthController.php:149](backend/app/Http/Controllers/AuthController.php:149))। তাই impersonate করা admin সেলারের পাসওয়ার্ড বদলে স্থায়ী দখল নিতে পারে না। টোকেন ৬০ মিনিটে মেয়াদোত্তীর্ণ, admin-কে impersonate করা নিষিদ্ধ (403), এবং টোকেনের নাম `impersonation:admin-{id}` হওয়ায় `personal_access_tokens`-এ স্থায়ী ট্রেইল থাকে।

### ১.৩ ইমেইল ও পেমেন্ট কলব্যাকে Host-header injection নেই
`FrontendUrl` ইচ্ছাকৃতভাবে **ব্যবহারকারী থেকে** ঠিকানা resolve করে, রিকোয়েস্টের Host থেকে নয়। এই URL গুলো ইমেইলে ও bKash/Facebook কলব্যাকে যায় — Host-নির্ভর হলে প্রতিটাই open redirect / phishing ভেক্টর হতো। একটি ডেডিকেটেড টেস্ট spoofed Host দিয়ে এটি যাচাই করে।

### ১.৪ সাবডোমেইন হাইজ্যাক প্রতিরোধ
- **Tombstone**: ছেড়ে দেওয়া label চিরকাল সংরক্ষিত; অন্য সেলার নিতে পারে না, তাই আগের সেলারের বিজ্ঞাপন-ট্রাফিক উত্তরাধিকার সূত্রে পাওয়ার পথ নেই।
- **is_system সুরক্ষা**: DNS-এ বিদ্যমান label (`mail`, `cpanel`, `bsol`…) UI থেকে মুক্ত করা যায় না। মুক্ত করা গেলে সেলার শপের ইমেইল বা হোস্টিং প্যানেল দখল করতে পারত — explicit DNS রেকর্ড সবসময় wildcard-কে হারায়, ফলে ট্র্যাফিক আসল হোস্টেই যেত অথচ BSOL ভাবত label-টা সেলারের।
- **Race condition**: availability চেক ও সেভের মাঝে DB unique index-ই চূড়ান্ত সালিশ; `QueryException` ধরা হয়, তাই একসাথে দুই সেলার একই label দাবি করলে পরিষ্কার 422 আসে।

### ১.৫ ক্রস-শপ ডেটা ফাঁস নেই
লাইভ যাচাই: `zisan.zyrotechbd.com/watch` → **404** (অন্য শপের পেজ), `nosuchshop-zz.zyrotechbd.com/dashboard` → **404**, `nosuchshop-zz.../api/public/landing-pages/watch` → **404**। মালিকবিহীন সাবডোমেইন কোনো ল্যান্ডিং পেজ resolve করে না।

### ১.৬ Homograph/unicode দিয়ে reserved তালিকা বাইপাস হয় না
`SubdomainPolicy::normalize()` `[a-z0-9-]`-এর বাইরের সব অক্ষর ফেলে দেয়। সিরিলিক `а` দিয়ে `mаil` লিখলে সেটা `mil` হয়ে যায় — `mail`-এর সংরক্ষণ বাইপাস হয় না।

---

## ২. পাওয়া ত্রুটি

### M-1 (Medium) — `x-bsol-shop-subdomain` হেডার জাল করা যেত — **ঠিক করা হয়েছে**

**যা ছিল:** proxy এই হেডারটি সেট করে এবং downstream কোড এর উপস্থিতিকে "proxy একটি বৈধ সেলার সাবডোমেইন resolve করেছে" প্রমাণ হিসেবে ধরে। কিন্তু প্ল্যাটফর্ম host-এ (`bsol.zyrotechbd.com`) proxy আগেভাগেই `NextResponse.next()` করে বেরিয়ে যেত — **ইনবাউন্ড হেডার মুছত না**। ফলে যেকোনো ক্লায়েন্ট নিজেই `x-bsol-shop-subdomain: zareen` পাঠিয়ে অ্যাপকে বিভ্রান্ত করতে পারত।

**আজকের প্রকৃত প্রভাব:** সীমিত — হেডারটি এখন শুধু `lp/[slug]/page.tsx`-এ canonical রিডাইরেক্টের সিদ্ধান্তে ব্যবহৃত হয়, আর `/lp/` পথ proxy-তেই 404 হয়। কেউ বড়জোর নিজের জন্য একটা রিডাইরেক্ট দমন করতে পারত।

**কেন তবুও Medium:** এটি একটি **সুপ্ত বিপদ**। হেডারটি কোডে "proxy-set, trusted" হিসেবে নথিভুক্ত। ভবিষ্যতে কেউ এর উপর ভিত্তি করে অনুমোদন সিদ্ধান্ত নিলে (যেমন "এই হেডার থাকলে সেলারের ডেটা দেখাও") সেটি সরাসরি ক্রস-শপ ডেটা ফাঁসে পরিণত হতো — এবং কোড পড়ে সেটা ভুল মনে হতো না।

**সংশোধন:** proxy এখন প্ল্যাটফর্ম-host পথেও ইনবাউন্ড হেডার মুছে দেয়; হেডারের নাম একটি নামযুক্ত ধ্রুবকে (`SHOP_HEADER`) সরিয়ে মন্তব্যে স্পষ্ট করা হয়েছে যে এটি কখনো ক্লায়েন্ট থেকে গ্রহণযোগ্য নয়। লাইভ যাচাই: জাল হেডার সহ রিকোয়েস্ট স্বাভাবিক 200 দেয়, হেডার অ্যাপে পৌঁছায় না।

### M-2 (Medium) — নির্বিচার `Host` হেডার গ্রহণ করা হতো — **ঠিক করা হয়েছে**

**যা ছিল:** Laravel-এ `trustHosts` সক্রিয় ছিল না। Laravel Host থেকে absolute URL তৈরি করে — যেমন [WordpressApiKeyController.php:171](backend/app/Http/Controllers/Api/WordpressApiKeyController.php:171)-এর `url('/api/wordpress/plugin-download')` — তাই অচেনা Host রেসপন্সে প্রতিফলিত হতো।

**প্রশমক পরিস্থিতি (যাচাইকৃত):** nginx-এর কারণে বাস্তবে শুধু `*.zyrotechbd.com` হোস্টই TLS পেরিয়ে আসতে পারত (wildcard cert), আর প্লেইন HTTP 301 হয়ে যেত। তাই বাইরের ডোমেইন প্রতিফলিত করানো কঠিন ছিল। এছাড়া ইমেইল/কলব্যাকের URL গুলো Host-নির্ভর নয় (§১.৩), তাই সবচেয়ে বিপজ্জনক পথটি আগে থেকেই বন্ধ ছিল।

**সংশোধন:** `bootstrap/app.php`-এ `trustHosts` চালু — apex ও তার সাবডোমেইন ছাড়া সব Host প্রত্যাখ্যাত। লাইভ যাচাই: বৈধ host 200, `Host: evil.com` → **400**। পূর্ণ টেস্ট সুইট (১৭২ পাস) এতে ভাঙেনি।

---

## ৩. গ্রহণযোগ্য ঝুঁকি ও সুপারিশ

### L-1 (Low) — Handoff কোড URL-এ যায়
লগইন handoff-এর একবার-ব্যবহারযোগ্য কোড URL query-তে ভ্রমণ করে, তাই ব্রাউজার হিস্ট্রি/রেফারার/প্রক্সি লগে পড়তে পারে।

**প্রশমন যা আছে:** কোডে টোকেন নেই; ৬০ সেকেন্ড TTL; `Cache::pull` দিয়ে সত্যিকারের single-use; গন্তব্য host-এ বাঁধা।

**সৎ মূল্যায়ন:** host binding **অন্য origin-এ replay** ঠেকায়, কিন্তু যে আক্রমণকারী কোডটি ৬০ সেকেন্ডের মধ্যে হাতে পেয়েছে সে সঠিক host দিয়েই সেটি ভাঙাতে পারবে — এটি URL-বাহিত যেকোনো কোডের অন্তর্নিহিত সীমা। প্রকৃত সুরক্ষা হলো TTL ও single-use। গ্রহণযোগ্য, তবে ডকুমেন্টেশনে এই সীমাটি স্পষ্ট থাকা উচিত (এই রিপোর্টে করা হলো)।

### L-2 (Low) — Impersonation সেশন আগেভাগে বাতিল করার উপায় নেই
টোকেন ৬০ মিনিট বাঁচে; admin "ফিরে যান" চাপলে ক্লায়েন্ট-সাইডে টোকেন ফেলে দেওয়া হয়, কিন্তু সার্ভারে টোকেনটি বাতিল হয় না। সেলারকেও জানানো হয় না যে তার অ্যাকাউন্ট দেখা হয়েছে।

**সুপারিশ:** (ক) "ফিরে যান"-এ সার্ভার-সাইড token revoke যোগ করা, (খ) সেলারের অ্যাকাউন্টে "সাপোর্ট আপনার অ্যাকাউন্ট দেখেছে" নোটিফিকেশন — স্বচ্ছতার জন্য। দুটোই ছোট কাজ, এখনো করা হয়নি।

### L-3 (Low) — Impersonation টোকেনের ক্ষমতা পূর্ণ (`['*']`)
Impersonate করা admin সেলার যা করতে পারে সবই করতে পারে — যেমন WordPress API key পুনরায় তৈরি করা (যা সেলারের সাইট সাময়িকভাবে বিচ্ছিন্ন করে দিতে পারে)। পাসওয়ার্ড বদলানো যায় না (§১.২)।

**সুপারিশ:** পরে দরকার হলে read-only ability scope যোগ করা যায়। বর্তমানে ট্রেইল (টোকেন নাম + লগ) থাকায় গ্রহণযোগ্য।

### L-4 (Low) — দৃষ্টিভ্রম-সদৃশ সাবডোমেইন (`rnail`, `paypaI`)
Reserved তালিকা প্রযুক্তিগত বাইপাস ঠেকায়, কিন্তু দেখতে-একরকম নাম ঠেকায় না। কোনো সেলার `secure-login` বা `rnail`-এর মতো নাম নিয়ে ফিশিং পেজ বানাতে পারে — এবং সেটি আমাদের বৈধ TLS সার্টিফিকেটে চলবে।

**প্রশমন যা আছে:** সাবডোমেইন নিতে সক্রিয় সাবস্ক্রিপশনসহ অ্যাকাউন্ট লাগে (পরিচয় ও অর্থপ্রবাহের ট্রেইল থাকে), আর admin যেকোনো সময় label reserve করে ভবিষ্যতের দাবি ঠেকাতে পারে।

**সুপারিশ:** নতুন সাবডোমেইন দাবির একটা periodic রিভিউ, এবং প্রয়োজনে `login`, `secure`, `verify`, `account` জাতীয় শব্দাংশ-ভিত্তিক ব্লক তালিকায় যোগ করা (এখন UI থেকেই সম্ভব)।

---

## ৪. তথ্যমূলক পর্যবেক্ষণ

### I-1 — `www.{seller}.{apex}` সার্টিফিকেট ত্রুটি দেখাবে
Let's Encrypt wildcard এক লেভেল কভার করে (`*.{apex}`), `*.*.{apex}` ইস্যু করা যায় না। কেউ `www.` সহ লিখলে ব্রাউজার সতর্কতা দেখাবে। **এটি নিরাপত্তা ত্রুটি নয়** (TLS ঠিকভাবেই ব্যর্থ হচ্ছে), কিন্তু ব্যবহারকারীকে সার্টিফিকেট সতর্কতা উপেক্ষা করতে অভ্যস্ত করা নিজেই ঝুঁকিপূর্ণ। সেলারদের কোথাও `www.` সহ ঠিকানা দেখানো যাবে না।

### I-2 — CORS এখনো `Access-Control-Allow-Origin: *`
Bearer টোকেন `localStorage`-এ থাকায় ও `supports_credentials` বন্ধ থাকায় এটি টোকেন ফাঁসের পথ নয় — কোনো তৃতীয় সাইট আমাদের টোকেন পড়তে পারে না। তবু সেলার সাবডোমেইনের সেট এখন জানা, তাই `^https://[a-z0-9-]+\.zyrotechbd\.com$` প্যাটার্নে সংকুচিত করা defense-in-depth হিসেবে সুপারিশযোগ্য।

### I-3 — সাবডোমেইন enumeration সম্ভব
`/api/public/shop-by-subdomain/{label}` দিয়ে কোন label নেওয়া আছে তা যাচাই করা যায় (throttle 120/min)। সাবডোমেইন স্বভাবতই পাবলিক (DNS ও TLS-এ দৃশ্যমান), তাই এটি প্রকৃত ফাঁস নয়।

---

## ৫. অপরিবর্তনীয় নিরাপত্তা শর্ত

ভবিষ্যতের যেকোনো পরিবর্তনে নিচের তিনটি ভাঙা যাবে না — ভাঙলে §১.১-এর পুরো যুক্তি ধসে পড়ে:

1. **`SESSION_DOMAIN` কখনো `.zyrotechbd.com` করা যাবে না।**
2. **Auth টোকেন কখনো `localStorage` থেকে কুকিতে সরানো যাবে না** — কুকি ডোমেইন-স্কোপড, `localStorage` origin-স্কোপড; পার্থক্যটাই এখানে নিরাপত্তা।
3. **ট্র্যাকিং কুকি exact-host-এ**, `domain=` অ্যাট্রিবিউট ছাড়া।

সাথে এই অডিট থেকে যোগ:

4. **`x-bsol-shop-subdomain` কখনো ক্লায়েন্ট থেকে গ্রহণ করা যাবে না** — proxy-র বাইরে কোথাও এর উপর অনুমোদন সিদ্ধান্ত নেওয়া যাবে না।
5. **`reserved_subdomains.is_system` row মুছে ফেলার কোনো পথ তৈরি করা যাবে না।**
6. **`zyrotechbd.com`-এ নতুন DNS রেকর্ড যোগ করলে সেই label সাথে সাথে reserved তালিকায় যোগ করতে হবে** (Admin → Settings → Reserved Subdomains)।

---

## ৬. এই অডিটে করা পরিবর্তন

| পরিবর্তন | ফাইল |
|---|---|
| প্ল্যাটফর্ম host-এ ইনবাউন্ড `x-bsol-shop-subdomain` হেডার মুছে ফেলা | `frontend/src/proxy.ts` |
| `trustHosts` সক্রিয় — অচেনা Host প্রত্যাখ্যান | `backend/bootstrap/app.php` |

**যাচাই:** পূর্ণ টেস্ট সুইট **১৭২ পাস** (পরিচিত ২টি pre-existing failure অপরিবর্তিত), `tsc` ও eslint পরিষ্কার, লাইভে বৈধ host 200 / অবৈধ host 400, জাল হেডার আর অ্যাপে পৌঁছায় না, সেলার সাবডোমেইন ও ল্যান্ডিং পেজ অক্ষত।
